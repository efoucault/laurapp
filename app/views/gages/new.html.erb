<%= simple_form_for @gage do |f| %>
  <!-- [...] -->
  <%= f.input :name %>
  <%= f.input :photo %>
  <%= f.input :photo_cache, as: :hidden %>
  <%= f.input :audio %>
  <%= f.input :audio_cache, as: :hidden %>
  <%= f.submit "Enregistrer le gage", class: "btn btn-success"%>

  <!-- [...] -->
<% end %>
